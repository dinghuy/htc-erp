<!-- Parent: ../AGENTS.md -->
# docs

Parent `AGENTS.md` guidance applies unless this file narrows it for `docs`.

## Scope Notes

- Keep this file stable and directory-specific; do not add generated file inventories or temp artifacts here.
- `docs/index.md` is the entrypoint for active canonical docs and workstreams.
- Prefer updating existing canonical docs over creating new top-level files.
- Put current implementation plans under `docs/workstreams/`, not at the top level of `docs/`.
- Treat historical or generated report material as reference-only unless an active canonical doc promotes it explicitly.
- Do not read from `archive/docs-reference/` during normal work unless the user explicitly asks for historical material.
- When docs describe shipped behavior, include links to verification evidence or tracked decisions where relevant.
