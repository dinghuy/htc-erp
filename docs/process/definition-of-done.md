# Definition Of Done

A task is done only when:

- The requested change is implemented inside the correct module boundary.
- Verification commands were run successfully after the change.
- Any changed API or workflow is reflected in docs or UAT notes.
- No generated runtime artifacts were added to version control.
- The change stays bounded and does not hide unrelated rewrites.
