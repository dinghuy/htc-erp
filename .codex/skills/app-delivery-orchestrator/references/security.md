# Security Guardrails

- Keep external writes human-directed.
- Stop when auth is missing and name the server that needs login.
- Treat Figma as design source, not business-rule source.
- Use Playwright for evidence, not as a substitute for backend authorization logic.
